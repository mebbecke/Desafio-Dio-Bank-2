import { Box } from "@chakra-ui/react"
import { DCard } from "../components/Card";

const Home = () => {
    return (
        <Box>
            <DCard id="login" />
        </Box>
    );
}

export default Home;